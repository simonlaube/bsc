class LlssbType:
    STD_ENCRYPTION = 0
    SSB_LOG = 1
    
def encode(msg):
    """Encodes message into one or multiple packets"""

def pack_128():
    """Returns one Packet of size up to 48B"""


def decode():
    pass
